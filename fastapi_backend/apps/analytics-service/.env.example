JWT_SECRET=your_shared_jwt_secret_here
JUDGE_API_KEY=softec_judge_2026

# Node backend API URLs (update for production)
EARNINGS_SERVICE_URL=http://localhost:3002
GRIEVANCE_SERVICE_URL=http://localhost:3004

# Long-lived admin JWT minted by auth-service for internal service-to-service calls.
# Generate: node -e "require('jsonwebtoken').sign({sub:'analytics-svc',role:'admin'},'<JWT_SECRET>',{noTimestamp:true},(e,t)=>console.log(t))"
INTERNAL_SERVICE_TOKEN=

# CORS (comma-separated)
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
